/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AlignmentDrawerDirective } from './alignment-drawer.directive';

describe('Directive: VariantDrawer', () => {
  it('should create an instance', () => {
    const directive = new AlignmentDrawerDirective();
    expect(directive).toBeTruthy();
  });
});
